{
  "name": "validai",
  "version": "1.0.0",
  "description": "Instant browser-based syntax validator for YAML, JSON, TOML, XML, INI, and .env files.",
  "scripts": {
    "test": "node tests/validators.test.js",
    "test:install": "npm install js-yaml @xmldom/xmldom && npm test"
  },
  "devDependencies": {
    "@xmldom/xmldom": "^0.9.12",
    "js-yaml": "^4.1.1"
  },
  "keywords": [
    "validator",
    "yaml",
    "json",
    "toml",
    "xml",
    "ini",
    "env",
    "syntax-checker"
  ],
  "license": "MIT"
}
